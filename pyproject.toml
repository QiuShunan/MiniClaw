[project]
name = "miniclaw"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "‘qiushunan’", email = "‘2383370396@qq.com’" }
]
requires-python = ">=3.10,<3.14"
dependencies = [
    "apscheduler==3.11.2",
    "claude-agent-sdk==0.1.50",
    "lark-oapi==1.5.4",
    "python-dotenv>=1.2.2",
    "qq-botpy==1.2.1",
]

[project.scripts]
miniclaw = "miniclaw.main:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
